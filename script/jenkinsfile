pipeline {
    agent any
    tools{
      maven 'M2_HOME'
    }
    stages {
        stage('maven10') {
            steps{
            sh 'mvn clean validate compile install test package'
            }
        }
    }
}
